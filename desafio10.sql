SELECT prod.ProductName AS `Produto`, MIN(ordeDet.Quantity) AS `Mínima`, MAX(ordeDet.Quantity) AS `Máxima`, CAST(AVG(ordeDet.Quantity) AS DECIMAL(10, 2)) AS `Média` FROM w3schools.products AS prod INNER JOIN order_details AS ordeDet ON prod.ProductID = ordeDet.ProductID GROUP BY `Produto` HAVING AVG(ordeDet.Quantity) > 20.00 ORDER BY `Média`, `Produto`; -- Usei o CAST para selecionar as casas decimais - tirei do link: https://www.w3resource.com/sql/aggregate-functions/avg-decimal-places-using-cast-within-avg.php